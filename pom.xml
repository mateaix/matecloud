<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.8</version>
        <relativePath/>
    </parent>

    <groupId>vip.mate</groupId>
    <artifactId>matecloud</artifactId>
    <version>5.0.8</version>
    <packaging>pom</packaging>
    <name>MateCloud</name>
    <description>MateCloud DDD Microservice Scaffold</description>
    <url>https://github.com/matevip/matecloud</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:https://github.com/matevip/matecloud.git</connection>
        <developerConnection>scm:git:https://github.com/matevip/matecloud.git</developerConnection>
        <url>https://github.com/matevip/matecloud</url>
    </scm>

    <organization>
        <name>MateCloud</name>
        <url>https://github.com/matevip/matecloud</url>
    </organization>

    <modules>
        <module>mate-bom</module>
        <module>mate-common</module>
        <module>mate-starters</module>
        <module>mate-starters-contrib</module>
        <module>mate-gateway</module>
        <module>mate-auth</module>
        <module>mate-biz</module>

        <module>mate-cli</module>
    </modules>

    <properties>
        <!-- Java -->
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- MateCloud Module Version -->
        <matecloud.version>5.0.8</matecloud.version>

        <!-- Spring Ecosystem -->
        <spring-boot.version>4.0.8</spring-boot.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.1.0.0</spring-cloud-alibaba.version>
        <!-- Nacos client 单独升级:对齐 nacos-server 3.2.1(spring-cloud-alibaba BOM 默认 3.1.1)。
             BOM 仅管 nacos-client, 其 basic/auth/encryption 等兄弟件随它传递, 故只需 pin 这一个。 -->
        <nacos-client.version>3.2.1</nacos-client.version>

        <!-- RPC -->
        <dubbo.version>3.3.6</dubbo.version>

        <!-- ORM -->
        <mybatis-plus.version>3.5.16</mybatis-plus.version>
        <mybatis-plus-generator.version>3.5.16</mybatis-plus-generator.version>
        <dynamic-datasource.version>4.3.1</dynamic-datasource.version>

        <!-- Database -->
        <mysql.version>8.3.0</mysql.version>
        <druid.version>1.2.24</druid.version>
        <p6spy.version>3.9.1</p6spy.version>

        <!-- Security -->
        <sa-token.version>1.45.0</sa-token.version>

        <!-- Cache & Redis -->
        <redisson.version>4.5.0</redisson.version>

        <!-- Serialization & Doc -->
        <fastjson2.version>2.0.53</fastjson2.version>
        <smart-doc.version>3.0.9</smart-doc.version>

        <!-- Utils -->
        <lombok.version>1.18.36</lombok.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <hutool.version>5.8.35</hutool.version>
        <bouncycastle.version>1.84</bouncycastle.version>
        <zxing.version>3.5.3</zxing.version>
        <lark-oapi.version>2.7.1</lark-oapi.version>
        <guava.version>33.6.0-jre</guava.version>
        <commons-lang3.version>3.17.0</commons-lang3.version>
        <commons-io.version>2.18.0</commons-io.version>
        <commons-collections4.version>4.5.0-M3</commons-collections4.version>
        <easyexcel.version>4.0.3</easyexcel.version>
        <transmittable-thread-local.version>2.14.5</transmittable-thread-local.version>

        <!-- OSS -->
        <minio.version>8.5.14</minio.version>
        <aws-s3.version>2.46.17</aws-s3.version>

        <!-- Logging -->
        <logstash-logback.version>9.0</logstash-logback.version>

        <!-- Spring AI (targets Spring Boot 4.x) -->
        <spring-ai.version>2.0.1</spring-ai.version>

        <!-- Build Plugin Versions -->
        <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
        <spring-boot-maven-plugin.version>${spring-boot.version}</spring-boot-maven-plugin.version>
        <smart-doc-maven-plugin.version>3.0.9</smart-doc-maven-plugin.version>
        <jacoco-maven-plugin.version>0.8.12</jacoco-maven-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ==================== BOM Imports ==================== -->
            <!-- Spring Boot first to ensure its Spring Framework 7.x baseline wins -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- nacos-client 单独升级到 3.2.1, 覆盖上面 spring-cloud-alibaba BOM 里的 3.1.1。
                 直接声明的 managed 依赖优先级高于 imported BOM。 -->
            <dependency>
                <groupId>com.alibaba.nacos</groupId>
                <artifactId>nacos-client</artifactId>
                <version>${nacos-client.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.dubbo</groupId>
                <artifactId>dubbo-bom</artifactId>
                <version>${dubbo.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ==================== MateCloud Modules: mate-common ==================== -->
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-base</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-api</artifactId>
                <version>${matecloud.version}</version>
            </dependency>

            <!-- ==================== MateCloud Modules: mate-starters ==================== -->
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-ds-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-web-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-nacos-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-rpc-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-menu-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-cache-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-monitor-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-sa-token-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-mq-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-job-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-seata-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-file-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-file-oss</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-sms-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-channel-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-tenant-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-sso-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-sharding-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-sentinel-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-security-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-gray-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-excel-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-flow-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-rule-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-test-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-ai-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>
            <dependency>
                <groupId>vip.mate</groupId>
                <artifactId>mate-model-starter</artifactId>
                <version>${matecloud.version}</version>
            </dependency>

            <!-- BouncyCastle -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>

            <!-- 飞书/Lark 官方 SDK -->
            <dependency>
                <groupId>com.larksuite.oapi</groupId>
                <artifactId>oapi-sdk</artifactId>
                <version>${lark-oapi.version}</version>
            </dependency>

            <!-- 二维码生成 (ZXing) -->
            <dependency>
                <groupId>com.google.zxing</groupId>
                <artifactId>core</artifactId>
                <version>${zxing.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.zxing</groupId>
                <artifactId>javase</artifactId>
                <version>${zxing.version}</version>
            </dependency>

            <!-- ==================== Third-party Dependencies ==================== -->
            <!-- MyBatis Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-annotation</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-generator</artifactId>
                <version>${mybatis-plus-generator.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>dynamic-datasource-spring-boot3-starter</artifactId>
                <version>${dynamic-datasource.version}</version>
            </dependency>

            <!-- Database -->
            <dependency>
                <groupId>com.mysql</groupId>
                <artifactId>mysql-connector-j</artifactId>
                <version>${mysql.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>
            <dependency>
                <groupId>p6spy</groupId>
                <artifactId>p6spy</artifactId>
                <version>${p6spy.version}</version>
            </dependency>

            <!-- Sa-Token -->
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-reactor-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-redis-jackson</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-core</artifactId>
                <version>${sa-token.version}</version>
            </dependency>

            <!-- Redisson -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>${redisson.version}</version>
            </dependency>

            <!-- Serialization -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson2.version}</version>
            </dependency>

            <!-- Utils -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-collections4</artifactId>
                <version>${commons-collections4.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>easyexcel</artifactId>
                <version>${easyexcel.version}</version>
            </dependency>
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>transmittable-thread-local</artifactId>
                <version>${transmittable-thread-local.version}</version>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- OSS -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${aws-s3.version}</version>
            </dependency>
            <!-- Lightweight sync HTTP client (JDK HttpURLConnection based, no Netty) for the S3 client -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>url-connection-client</artifactId>
                <version>${aws-s3.version}</version>
            </dependency>

            <!-- Logging -->
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok-mapstruct-binding</artifactId>
                                <version>0.2.0</version>
                            </path>
                        </annotationProcessorPaths>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot-maven-plugin.version}</version>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>com.ly.smart-doc</groupId>
                    <artifactId>smart-doc-maven-plugin</artifactId>
                    <version>${smart-doc-maven-plugin.version}</version>
                    <configuration>
                        <configFile>./src/main/resources/smart-doc.json</configFile>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <!-- 测试覆盖率: prepare-agent 给 surefire 挂 javaagent, report 在 test 后生成 HTML/XML。
                 每模块 target/site/jacoco/ 输出报告; 不引入门禁阈值 (check), 先观测后收紧, 零构建风险。 -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>monolith</id>
            <modules>
                <module>mate-monolith</module>
            </modules>
        </profile>
    </profiles>

    <repositories>
        <repository>
            <id>aliyun-maven</id>
            <name>Aliyun Maven Repository</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>aliyun-plugin</id>
            <name>Aliyun Plugin Repository</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>
